import {Server} from "./server";
import {Router} from "./router";
import {Configuration} from "./configuration";

class Streamer {
  constructor(configuration: Configuration, server: Server, router: Router) {

  }
}

export {
  Streamer
}
